import { Styles } from '#types/atoms.types';

type Sx = 'header';

export const baseLayoutStyles: Styles<Sx> = {
  header: {
    backgroundColor: 'transparent',
    border: 'none',
    borderRadius: 0
  }
};
